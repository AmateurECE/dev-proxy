[package]
name = "dev-prox"
version = "0.1.0"
edition = "2021"

[dependencies]
hyper = { version = "0.14", features = ["client", "server", "tcp", "http1"] }
tokio = { version = "1.17", features = ["full"] }
